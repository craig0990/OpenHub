{
    "dashboard": [
        "/",
        {
            "values": {
                "controller": "Dashboard"
            }
        }
    ],
    "repo": [
        "/repo/{:name}/{:path*}",
        {
            "values": {
                "controller": "Repository"
            },
            "params": {
                "name": "([A-Za-z0-9]+)"
            }
        }
    ]
}
